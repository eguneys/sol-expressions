{
  "name": "babel-plugin-jsx-sol-expressions",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "test": "yarn run build && jest",
    "watch": "rollup -cw & jest --watch",
    "test:coverage": "yarn run build && jest --coverage"
  },
  "dependencies": {
    "@babel/plugin-syntax-jsx": "^7.16.7"
  }
}
